import React, {Fragment} from 'react';

const Bird = (props) => {

		return (
		<Fragment>
    		<form>
                this is the bird form
            </form>
    	</Fragment>
		)
	}

export default Bird;